scripts:
  start: deno run --allow-net --allow-read --allow-env --unstable --import-map=./import_map.json mod.ts
  reload: deno run --reload --allow-net --allow-read --allow-env --unstable mod.ts
  fmt: deno fmt
